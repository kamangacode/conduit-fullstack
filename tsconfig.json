{
  "$schema": "https://json.schemastore.org/tsconfig",
  "//": "tsconfig racine pour l'éditeur uniquement. Le typecheck réel tourne par workspace via `turbo run typecheck`. Les références de projet sont ajoutées à mesure que les workspaces atterrissent (shared, api, web).",
  "extends": "./tsconfig.base.json",
  "files": [],
  "references": [{ "path": "./packages/shared" }]
}
