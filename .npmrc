# pnpm — résolution stricte et déterministe pour un monorepo vitrine.
# strict-peer-dependencies coupé le temps du bootstrap (NestJS/Next tirent des
# peers larges) ; à resserrer si l'arbre le permet.
strict-peer-dependencies=false
auto-install-peers=true
resolution-mode=highest
