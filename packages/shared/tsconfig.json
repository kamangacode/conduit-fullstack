{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "../../tsconfig.base.json",
  "_comment": "CommonJS pour rester consommable sans friction par NestJS (apps/api) comme par Next.js (apps/web). composite active les références de projet depuis la racine.",
  "compilerOptions": {
    "module": "commonjs",
    "moduleResolution": "node",
    "composite": true,
    "rootDir": "src",
    "outDir": "dist",
    "declarationMap": true
  },
  "include": ["src/**/*.ts"],
  "exclude": ["dist", "node_modules", "**/*.spec.ts"]
}
